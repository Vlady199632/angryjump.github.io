/* Логіка гри */
Кінець гри
1 - програш:
    а) - не перестрибнув;
    б) - закінчився час;


2 - виграв:
    а) - якщо протримався 1хв;
    б) - якщо пройшов дистанцію;
    в) - якщо назбирав поінтів;

3 - початок:
    а) - натискання кнопки старт;
       - меню:
            .статистика;
4 - проходження гри
    а)Управління:
        1.Кнопка пробіл - стрибок;
        2.Кнопка паузи;
    б)Дизайн(перешкоди, бонуси, перешкоди,);


/*Дизайн*/
    а) - фото + Css;

/* Етапи */
-   при відкритті бачу меню(html, css, button = onclick + js(function start());
-   зявляється персонаж дорога та перешкода пробіл (function jump(keyspace))
-   програш персонаж попав на кактус (мають однакові координати)  запускається функція function gameOver();
-   перемога - function finish();---Рівень пройдено - alert;

